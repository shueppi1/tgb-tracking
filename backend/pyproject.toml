[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# Make `app` and `tests` importable whether run as `pytest` or `python -m pytest`.
pythonpath = ["."]
